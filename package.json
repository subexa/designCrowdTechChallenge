{
  "devDependencies": {
    "@testing-library/cypress": "^10.0.1",
    "cypress": "^13.6.6",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "cy:run:chrome": "cypress run --browser chrome",
    "cy:run:firefox": "cypress run --browser firefox",
    "cy:open": "cypress run --browser chrome --headed"
  }
}
